version: '3'
services:
  database:
    image: postgres:latest
    restart: always
    ports:
      - 5432:5432
    env_file:
      - .env
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080
